import styled from "styled-components";

const HeaderBox = styled.div`
  margin-bottom: 40px;
  display: inline-flex;
  align-items: center;

  .icon {
    position: absolute;
    right: 10px;
    top: 10px;
    cursor: pointer;
  }
`;

export default HeaderBox;
