import styled from "styled-components";

const ToggleBox = styled.div`
  display: inline;
  position: absolute;
  top: 10px;
  right: 10px;

  :hover {
    cursor: pointer;
  }
`;

export default ToggleBox;
