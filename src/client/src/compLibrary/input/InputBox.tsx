import styled from "styled-components";

const InputBox = styled.div`
  display: flex;
  
  /* &:before,
  &:after {
    content: " ";
    display: table;
  }
  &:after {
    clear: both;
  } */
`;

export default InputBox;
