import styled from "styled-components";

const NodeBox = styled.div`
  position: relative;
  height: inherit;
  width: inherit;
  max-height: inherit;

  img {
    position: relative;
    bottom: 15px;
    pointer-events: none;
  }
`;

export default NodeBox;
