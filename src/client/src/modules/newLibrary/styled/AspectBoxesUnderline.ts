import styled from "styled-components";
import { Color } from "../../../compLibrary/colors";

const AspectBoxesUnderline = styled.div`
  background-image: linear-gradient(to right, ${Color.FunctionMain}, ${Color.ProductMain}, ${Color.LocationMain});
  width: inherit;
  height: 2px;
`;

export default AspectBoxesUnderline;
