import styled from "styled-components";

const LibElementVersion = styled.span`
  flex: 1;
  position: absolute;
  right: 55px;
  text-align: right;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
`;

export default LibElementVersion;
