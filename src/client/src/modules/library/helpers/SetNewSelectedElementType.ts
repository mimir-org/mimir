/* eslint-disable @typescript-eslint/no-explicit-any */
import { ObjectType } from "../../../models";

export const SetNewSelectedElementType = (libraryType: ObjectType, setSelectedElementType: any) =>
  setSelectedElementType(libraryType);
