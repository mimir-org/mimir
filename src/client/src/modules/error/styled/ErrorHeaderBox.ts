import styled from "styled-components";

const ErrorHeaderBox = styled.div`
  display: inline-flex;
  align-items: center;

  .icon {
    position: absolute;
    right: 8px;
    top: 8px;
    cursor: pointer;
  }
`;

export default ErrorHeaderBox;
