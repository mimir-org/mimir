import styled from "styled-components";

const InspectorDrag = styled.div`
  position: absolute;
  height: 44px;
  width: 100%;

  &:hover {
    cursor: n-resize;
  }
`;

export default InspectorDrag;
