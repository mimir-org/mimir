import styled from "styled-components";

const ToggleBox = styled.div`
  display: inline;
  position: absolute;
  top: 14px;
  right: 12px;

  :hover {
    cursor: pointer;
  }
`;

export default ToggleBox;
