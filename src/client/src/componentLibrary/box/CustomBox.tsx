import styled from "styled-components";

const CustomBox = styled.div`
  margin-bottom: 40px;
  display: inline-flex;
  align-items: center;

  .icon {
    padding-right: 5px;
    cursor: pointer;
  }
`;

export default CustomBox;
