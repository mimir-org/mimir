export const OnMouseEnter = (setVisible: (visibility: boolean) => void) => {
  setVisible(true);
};

export const OnMouseLeave = (setVisible: (visibility: boolean) => void) => {
  setVisible(false);
};
