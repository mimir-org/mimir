import styled from "styled-components";

const Navigation = styled.div`
  display: inline-flex;
  position: relative;
  padding-right: 4px;

  &:hover {
    cursor: pointer;
  }
`;

export default Navigation;
