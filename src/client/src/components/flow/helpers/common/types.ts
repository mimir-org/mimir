export interface GetCenterParams {
  sourceX: number;
  sourceY: number;
  targetX: number;
  targetY: number;
}
export interface Legend {
  key: string;
  name: string;
  color: string;

  // TODO: En type striplet, pil etc.
}
