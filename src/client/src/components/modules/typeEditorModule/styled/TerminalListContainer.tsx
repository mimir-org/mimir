import styled from "styled-components";
import { Color } from "../../../../componentLibrary";

const TerminalListContainer = styled.div`
  div:nth-child(even) {
    background-color: ${Color.White};
  }
`;

export default TerminalListContainer;
