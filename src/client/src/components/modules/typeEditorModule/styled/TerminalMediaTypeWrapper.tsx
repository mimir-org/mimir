import styled from "styled-components";
import { Color, FontSize, FontType } from "../../../../componentLibrary";

const TerminalMediaTypeWrapper = styled.div`
  display: flex;
  flex-direction: row;
  flex: 1;
`;

export default TerminalMediaTypeWrapper;
