import styled from "styled-components";
import { Color } from "../../../../compLibrary/";

const TabsBottomLine = styled.div`
  border-bottom: 1px solid ${Color.Grey};
  flex-grow: 1;
`;

export default TabsBottomLine;
