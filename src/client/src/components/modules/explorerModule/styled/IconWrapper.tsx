import styled from "styled-components";

const IconWrapper = styled.div`
  margin-right: 120px;
  font-family: roboto;
  color: #000;
  font-size: 18px;
  font-weight: 700;
  display: inline-flex;
  align-items: center;
`;

export default IconWrapper;
