import styled from "styled-components";

const ErrorHeaderBox = styled.div`
  display: inline-flex;
  align-items: center;

  .icon {
    position: absolute;
    right: 5px;
    top: 5px;
    width: 18px;
    height: 18px;
    cursor: pointer;
  }
`;

export default ErrorHeaderBox;
