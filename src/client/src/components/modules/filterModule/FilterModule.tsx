import textResources from "../../../textResources";
import VisualFilterWrapper from "./styled/VisualFilterWrapper";

const FilterModule = () => {
  return (
    <VisualFilterWrapper>{textResources.Filter_Types}</VisualFilterWrapper>
  );
};

export default FilterModule;
