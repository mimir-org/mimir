import { Dispatch } from "redux";
// import { changeActiveMenu } from "../../redux/menuSlice";

const OnSubProjectReturnClick = (dispatch: Dispatch) => {
  // dispatch(changeActiveMenu(null));
};

export default OnSubProjectReturnClick;
