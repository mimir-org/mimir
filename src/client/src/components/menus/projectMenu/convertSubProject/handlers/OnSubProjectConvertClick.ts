// import { changeActiveMenu } from "../../redux/menuSlice";
// import { convertSubProjectStatus } from "../../../../../../../redux/store/project/actions";
import { Dispatch } from "redux";

const OnSubProjectConvertClick = (projectId: string, dispatch: Dispatch) => {
  // dispatch(changeActiveMenu(null));
  if (projectId != null) {
    //  dispatch(convertSubProjectStatus(projectId));
  }
};

export default OnSubProjectConvertClick;
